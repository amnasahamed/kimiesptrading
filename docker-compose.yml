version: '3.8'

services:
  trading-bot:
    build: .
    container_name: trading-bot
    restart: always
    ports:
      - "8000:8000"
    volumes:
      - ./config.json:/app/config.json
      - ./trades_log.json:/app/trades_log.json
    environment:
      - PYTHONUNBUFFERED=1
